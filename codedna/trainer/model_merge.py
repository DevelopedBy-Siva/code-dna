"""Model merge stubs for CodeDNA."""

from __future__ import annotations


def merge_adapter(checkpoint_path: str, output_path: str) -> None:
    """Merge a LoRA adapter into a base model."""

    _ = (checkpoint_path, output_path)
