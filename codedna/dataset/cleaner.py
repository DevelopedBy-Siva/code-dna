"""Dataset cleaning stubs for CodeDNA."""

from __future__ import annotations


def clean_pairs(pairs: list[dict], min_score: int = 60) -> list[dict]:
    """Clean dataset pairs prior to export."""

    _ = (pairs, min_score)
    return []
