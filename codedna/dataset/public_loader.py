"""Public dataset loader stubs for CodeDNA."""

from __future__ import annotations


def load_public_datasets(language: str = "python", max_samples: int = 5000) -> list[dict]:
    """Load normalized public datasets for CodeDNA."""

    _ = (language, max_samples)
    return []
